The scripts in these folders create json files which follow a deprecated index structure. 
The deprecated json files were updated by running these scripts and then running:

```bash
$ update_index.py
```